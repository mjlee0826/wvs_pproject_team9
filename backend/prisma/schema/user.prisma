model User {
  id          String    @id
  displayName String
  email       String    @unique
  avatar      String?
  role        String    @default("student")
  coins       Int       @default(0)
  posts       Post[]
  comments    Comment[]
  likes       Like[]
  messages    Message[]
  threads     Thread[]
  answers     Answer[]
  replies     Reply[]
  upvotes     Upvote[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}
